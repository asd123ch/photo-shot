// Placeholder for local development.
// In production this file is overwritten on container start with values from
// environment variables (see docker-entrypoint config script).
// In local dev, config values come from .env (VITE_* vars) — see config.ts.
window.__APP_CONFIG__ = {};
